version: '3.8'

services:
  yt-alert-bot:
    build: .
    container_name: yt-livechat-alert
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
      - AUTO_START_BOT=true
